cmake_minimum_required(VERSION 3.0)
project(merger)

# -----------------------------------------------------------------------------
# DO NOT EDIT THIS FILE
# -----------------------------------------------------------------------------

# If you want to add source or test files, add them
# to ‹src/CMakeLists.txt› or ‹test/CMakeLists.txt› respectively.

set(CMAKE_BUILD_TYPE Debug)

# Configure compiler warnings
if(CMAKE_C_COMPILER_ID MATCHES Clang OR ${CMAKE_C_COMPILER_ID} STREQUAL GNU)
    # using regular Clang, AppleClang or GCC
    # Strongly suggested: enable -Werror
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -std=c99 -pedantic -Wall -Wextra -Werror")
elseif(${CMAKE_C_COMPILER_ID} STREQUAL MSVC)
    # using Visual Studio C++
    set(CMAKE_C_FLAGS "/permissive- /W4 /EHsc")
endif()

add_subdirectory(src)
add_subdirectory(test)
